{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities the sign-in webview needs — invoke the Rust `sign_in` command + store persistence for non-secret app state.",
  "windows": ["signin", "main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-hide",
    "core:window:allow-show",
    "store:default",
    "log:default"
  ]
}
