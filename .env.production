# Frontend production env. nginx serves the PWA and reverse-proxies /api to the
# FastAPI backend on the same origin, so the API base is a relative path —
# no CORS, and the host/domain never needs to be hardcoded in the bundle.
VITE_API_URL=/api
